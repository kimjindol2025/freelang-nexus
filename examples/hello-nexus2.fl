@mode(v)

@lang("rust")
---
#[no_mangle]
pub extern "C" fn double_it(x: i32) -> i32 { x * 2 }
---

@mode(python)

def greet(name):
    print("Hello from Python: " + name)

@mode(v)

fn main() -> i64 {
  let result = double_it(21)
  greet("Nexus 2")
  println(result)
  return 0
}
